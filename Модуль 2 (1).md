## ✔️ Настройте контроллер домена Samba DC на сервере BR-SRV </code>

<details>
<summary><strong>[ОПИСАНИЕ ЗАДАНИЙ]</strong></summary>

### Произведите `базовую` настройку устройств

- Имя домена au-team.irpo
- Введите в созданный домен машину HQ-CLI
- Создайте 5 пользователей для офиса HQ: имена пользователей формата hquser№ (например hquser1, hquser2 и т.д.)
- Создайте группу hq, введите в группу созданных пользователей
- Убедитесь, что пользователи группы hq имеют право аутентифицироваться на HQ-CLI
- Пользователи группы hq должны иметь возможность повышать
- привилегии для выполнения ограниченного набора команд: cat, grep, id.
- Запускать другие команды с повышенными привилегиями пользователи группы права не имеют.

</details>

<br/>

<details>
<summary><strong>Настройка </strong></summary>
  
<br/>

  - Для **Linux** используется команда: <strong>`hostnamectl set-hostname (имя устройства.au-team.irpo)`</strong>
  
  - Обновить имя можно введя команду: **`newgrp`**
### BR-SRV
```
apt install samba-ad-dc smbclient winbind krb5-user krb5-config
```
```
rm -f /etc/samba/smb.conf
rm -f /etc/krb5.conf
```
<br/>

```
samba-tool domain provision
```
```
Realm: AU-TEAM.IRPO
Domain: AU-TEAM
Server Role: dc
DNS backend: SAMBA_INTERNAL
DNS forwarder: 192.168.1.2
Administrator password: P@ssw0rd
```
Если не работает то надо
В host добаваить из 1 модуля dnsmasq а так же в resolv добавить айпи hq-srv
<br/>

```
systemctl enable --now samba
```
```
cp /var/lib/samba/private/krb5.conf /etc/krb5.conf
systemctl unmask samba-ad-dc
systemctl enable --now samba-ad-dc
```
<br/>

```
# Проверка Kerberos-аутентификации
kinit administrator@AU-TEAM.IRPO
klist
```
```
samba-tool domain info 127.0.0.1
samba-tool domain info 192.168.0.2
```
<br/>

### Cоздаем пользователей
```
samba-tool user add user1.hq P@ssw0rd

samba-tool user add user2.hq P@ssw0rd

samba-tool user add user3.hq P@ssw0rd

samba-tool user add user4.hq P@ssw0rd

samba-tool user add user5.hq P@ssw0rd
```

<br/>

### Создаём группу и добавляем туда пользователей
```
samba-tool group add hq
samba-tool group addmembers hq user1.hq
samba-tool group addmembers hq user2.hq
samba-tool group addmembers hq user3.hq
samba-tool group addmembers hq user4.hq
samba-tool group addmembers hq user5.hq
```
<br/>

### Ввод клиента на HQ-CLI в домен
```
echo nameserver 192.168.0.2 > /etc/resolv.conf
если не работает лучше вернутся и посмотреть
```
```
apt install -y realmd sssd sssd-tools libnss-sss libpam-sss adcli krb5-user samba-common-bin packagekit
```
```
realm discover au-team.irpo
realm join --user=administrator au-team.irpo
```
Добавляем дополнительные права пользоватлею HQ-CLI для возможности пользоваться командами realm без пользования root прав. Для этого переходим в каталог nano /home/usr1/.bashrc и прописываем следующие значения:
```
export PATH=$PATH:/usr/sbin
```
```
nsource ~/.bashrc
realm permit -g hq
```
После, прописываем права доступа на hq-cli в каталоге nano /etc/sssd/sssd.conf  Если su не работает (access_provider = permit) :
```
[sssd]
services = nss, pam
domains = au-team.irpo
config_file_version = 2

[domain/au-team.irpo]
ad_domain = au-team.irpo
krb5_realm = AU-TEAM.IRPO
realmd_tags = manages-system joined-with-adcli
cache_credentials = true
id_provider = ad
auth_provider = ad
access_provider = ad
chpass_provider = ad
ldap_id_mapping = true
use_fully_qualified_names = false
fallback_homedir = /home/%u
default_shell = /bin/bash
```
Задаём права "только для чтения" на этот файл
```
chmod 600 /etc/sssd/sssd.conf
```
Так же, настраиваем вход по поролю для всех, даже если заходят из-под  root . Для этого переходим в конфиги РАСКОМЕНТИТЬ
```
nano /etc/pam.d/su
auth       sufficient pam_wheel.so trust
```
```
systemctl restart sssd
pam-auth-update
systemctl restart sssd
su - user1.hq@au-team.irpo
```

</details>

## ✔️ ебаное самбо не работает </code>

<details>
<summary><strong>[Фикс samba мб поможет]</strong></summary>


```
rm -f /etc/krb5.conf
rm -f /etc/samba/smb.con
rm -rf /var/lib/samba/
host -t SRV _kerberos._tcp.au-team.irpo
mkdir /var/lib/samba/
export PATH=$PATH:/usr/sbin
```
<br/>
</details>

<br/>



## ✔️ ## Сконфигурируйте файловое хранилище на сервере HQ-SRV </code>

<br/>

<details>
<summary><strong>[ОПИСАНИЕ ЗАДАНИЙ]</strong></summary>

### Настройка `ISP`

- При помощи двух подключенных к серверу дополнительных дисков размером 1 Гб сконфигурируйте дисковый массив уровня 0
- Имя устройства – md1, при необходимости конфигурация массива размещается в файле /etc/mdadm.conf
- Создайте раздел, отформатируйте раздел, в качестве файловой системы используйте ext4
- Обеспечьте автоматическое монтирование в папку /raid
</details>

<br/>

<details>
<summary><strong>Настройка RAID 0<code></code></strong></summary>

Настройка динамической сетевой трансляции на _`ISP`_
A
Проверяем наличие дисков на машине:

```
lsblk
```

Устанавливаем необходимую утелиту:

```
apt install mdadm -y
```

Компелируем диски в рейд:

```
mdadm --create --verbose /dev/md1 --level=0 --raid-devices=2 /dev/sdb /dev/sdc
```

Создаём файл для коректного сюора рейда и заночим изминения:

```
mkdir -p /etc/mdadm
```

```
mdadm --detail --scan --verbose >> /etc/mdadm.conf
```

Разграничеваем пространстов диска:

```
mkfs.ext4 /dev/md1
```

Создаём точку монтирования:

```
mkdir /raid
```

Узнаём UUID масива, для внесения изминений в /etc/fstab

```
blkid /dev/md0
```
Прверяем, создался ли масив:

<img width="713" height="59" alt="image" src="https://i.yapx.ru/dzgu5.png" />




После, переходим в конфигурационный файл mdadm.conf и смотрим UUID масива (он будет отличаться от предыдущего)

```
nano /etc/mdadm.conf
```

Создайте скриншот UUID и сохраните на устройство, что бы была возможность вернуться к нему позже

Теперь записываем его файл /etc/fstab:

```
nano /etc/fstab
```

Вы должны привести его к такому виду:

<img width="404" height="28" alt="image" src="https://i.yapx.ru/dzgwg.png" />


После внесения изминений, нужно перезагрузить демона:

```
systemctl daemon-reload
```

Монтируем раздел:

```
mount -a
```

В завершение, обновите <code>initramfs </code> для обновления информации о запуске масива.

```
update-initramfs -u
```

</details>

</br>


## ✔️ Задание 3 Настройте сервер сетевой файловой системы (nfs) на HQ-SRV 

<details>
<summary><strong>[ОПИСАНИЕ ЗАДАНИЙ]</strong></summary>

### NFS

- В качестве папки общего доступа выберите /raid/nfs, доступ для чтения и записи исключительно для сети в сторону HQ-CLI
- На HQ-CLI настройте автомонтирование в папку /mnt/nf


</details>

<br/>

<details>
<summary><strong> Настройка на HQ-SRV </strong></summary>

Первым делом устанавливаем необходимую утилиту:

```
apt install nfs-kernel-server -y
```

После создаём необходимый каталог:

```
mkdir -p /raid/nfs
```
Выыдаём права на его чтение:

```
chmod 777 /raid/nfs
```

Переходим в конфиги экспорта:

```
nano /etc/exports
```

И прописываем в нём следующее значение (ip адресс устройства должен быть точным):

```
/raid/nfs  192.168.2.0/28(rw,sync,no_subtree_check)
```

## Если дальше по заданию не заработает, укажите точный IP

Применям изминения:

```
exportfs -a
```

Включаем сервис:

```
systemctl enable --now nfs-server
```

Перезапускаем:

```
systemctl restart nfs-kernel-server
```
</details>

<details>
<summary><strong> Настройка на HQ-CLI </strong></summary>

Устанавливаем утилиту:

```
apt install -y nfs-common autofs
```

Проверяем соедиение:

```
showmount -e 192.168.1.2
```

Должени быть следующий результат:

<img width="471" height="69" alt="image" src="https://i.yapx.ru/dzgwr.png" />

Далее, создаём папку для монтирования:

```
mkdir -p /mnt/nfs
```

Переходим к автозапуску при загрузке системы:

```
nano /etc/fstab
```

Добавляем следующую строку в файл:

```
192.168.1.2:/raid/nfs  /mnt/nfs  nfs  defaults,_netdev  0  0
```

Или

```
echo "192.168.1.2:/raid/nfs  /mnt/nfs  nfs  defaults,_netdev  0  0" >> /etc/fstab
```

Производим монтирование:

```
mount -a
```

Проверяем результат, должен быть вывод как на рисунке:

```
df -h | grep nfs
```

<img width="723" height="53" alt="image" src="https://i.yapx.ru/dzgw0.png" />


</details>

<details>
<summary><strong> Проверка работоспособности </strong></summary>

Помимо основных проверок, чт обыли сделаны выше:

```
showmount -e 192.168.1.2
```

```
df -h | grep nfs
```
УКАЖИТЕ ТОЧНЫЙ АЙПИ КЛИЕНТА ЕСЛИ НЕ РАБОТАЕТ
<img width="494" height="23" alt="image" src="https://i.yapx.ru/dzgw2.png" />





</details>


<br/>

### Задание 4 - NTP (ISP)

<details>
<summary><strong> Настройка на ISP </strong></summary>

<br/>

Устанавливаем необходимую утилиту:

```
apt install chrony -y
```

Переходим в конфиг:

```
nano /etc/chrony/chrony.conf
```
Добавить:

```
server pool.ntp.org iburst
allow 0.0.0.0/0
local stratum 5
```

На месте сервера указываем свой внешний ntp

Перезапускаем утилиту:

```
systemctl restart chrony
```

Проверяем что ISP синхронизировался с внешним мирои:

```
chronyc sources -v
```

</details>

<details>
<summary><strong> Настройка на остальных машинах (HQ-SRV, HQ-CLI, BR-RTR, BR-SRV) </strong></summary>

Так же производим установку утилиты:

```
apt install chrony -y
```

Редактируем конфиг (настраиваем ip в зависимости от подсети):

```
nano /etc/chrony/chrony.conf
```

```
server 172.16.10.1 iburst
```

```
server 172.16.20.1 iburst
```

Приминяем настройки:

```
systemctl restart chrony
```

</details>

<details>
<summary><strong> Проверка работы </strong></summary>

Прописываем следующие команды:

```
chronyc sources
```

```
chronyc tracking
```

Результат должен быть следующий:

<img width="705" height="384" alt="изображение" src="https://i.yapx.ru/dzgw4.png" />


  
</details>

<br/>

## ✔️ 5 ANsible

<details>
<summary><strong>[ОПИСАНИЕ ЗАДАНИЙ]</strong></summary>

### Настройте на интерфейсе `HQ-RTR` в сторону офиса `HQ` виртуальный коммутатор

- Сформируйте файл инвентаря, в инвентарь должны входить HQ-SRV, HQ-CLI, HQ-RTR и BR-RTR
- Рабочий каталог ansible должен располагаться в /etc/ansible
- Все указанные машины должны без предупреждений и ошибок отвечать pong на команду ping в ansible посланную с BR-SRV

</details>
<br/>

<details>
<summary><strong>ANSIBLAT</code></strong></summary>

- ## Для начала нужно установить ansible

**1.** КАЧАЕМ епт
```
apt install ansible sshpass -y
```
# Теперь создать ключ
```
ssh-keygen -t rsa
```
# Теперь это ключ нужно переместить на остальные машины командой
```
ssh-copy-id -p 2011 sshuser@192.168.1.2
ssh-copy-id locadm@192.168.2.2
ssh-copy-id net_admin@172.16.10.2
ssh-copy-id net_admin@172.16.20.2
```
```
mkdir /etc/ansible
nano /etc/ansible/hosts
```
<br/>

```
[hq]
192.168.1.2  ansible_user=sshuser  ansible_password=P@ssw0rd ansible_port=2011
192.168.2.2  ansible_user=locadm  ansible_password=P@ssw0rd
172.16.10.2  ansible_user=net_admin  ansible_password=P@ssw0rd

[br]
172.16.20.2  ansible_user=net_admin  ansible_password=P@ssw0rd
```
# Для проверки файла инвентаря используется команда 

```
ansible all -m ping
```
<img width="1337" height="697" alt="image" src="https://i.yapx.ru/dzgw6.png" />

</details>
<br/>

## ✔️ Задание 6 `[DOKERINO BANDITO]`

<details>
<summary><strong>[ОПИСАНИЕ ЗАДАНИЙ]</strong></summary>

### Настройка DOCKER на BR-SRV

- Средствами docker должен создаваться стек контейнеров с веб приложением и базой данных
- Используйте образы site_latestи mariadb_latestрасполагающиеся в директории docker в образе Additional.iso
- Основной контейнер testapp должен называться tespapp
- Контейнер с базой данных должен называться db
- Импортируйте образы в docker, укажите в yaml файле параметры подключения к СУБД, имя БД - testdb, пользователь testс паролем P@ssw0rd, порт приложения 8080, при необходимости другие параметры
- Приложение должно быть доступно для внешних подключений через порт 8080


</details>
<br/>

<details>
<summary><strong>Настройка <code></code></strong></summary>
<br/>

### DOCKER

Установка

```
apt install docker.io docker-compose -y
```

Создание директории монтирования

```
mkdir -p /mnt/iso
```

Монтирование

```
mount /dev/sr0 /mnt/iso
```

Перехол в точку монтирования

```
cd /mnt/iso/docker
```

Загрузка необходимых образов

```
docker load -i /mnt/iso/docker/site_latest.tar
```

```
docker load -i /mnt/iso/docker/mariadb_latest.tar
```

Проверка наличия образов

```
docker images
```

Создание рабочей лиректории и файла конфигурации

```
mkdir -p /opt/testapp && cd /opt/testapp
```

Перехол в файл конфигурации

```
nano docker-compose.yml
```

Конфиг
100 %
```
version: "3.9"

services:
    database:
    container_name: db
    image: mariadb:latest
    restart: always
    ports:
    - "3306:3306"
    environment:
    - MARIADB_USER=maria
    - MARIADB_PASSWORD=P@ssw0rd
    - MARIADB_DATABASE=mariadb
    - MARIADB_ROOT_PASSWORD=P@ssw0rd

app:
    container_name: testapp
    image: site:latest
    restart: always
    ports:
    - "8081:8080"
    depends_on:
    - database
    environment:
    - DB_HOST=databas
    - DB_PORT=3306
    - DB_NAME=mariadb
    - DB_USER=maria
    - DB_PASS=P@ssw0rd
    - DB_TYPE=maria
```
ХУЙ знает
```
version: "3.9"

services:
  database:
    container_name: db
    image: mariadb:latest
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MARIADB_USER=test1c
      - MARIADB_PASSWORD=P@ssw0rd
      - MARIADB_DATABASE=testdb1
      - MARIADB_ROOT_PASSWORD=P@ssw0rd

  app:
    container_name: testapp
    image: site:latest
    restart: always
    ports:
      - "8081:8080"
    depends_on:
      - database
    environment:
      - DB_HOST=database
      - DB_PORT=3306
      - DB_NAME=testdb1
      - DB_USER=test1c
      - DB_PASS=P@ssw0rd
      - DB_TYPE=mariadb
```
<img width="975" height="655" alt="image" src="https://i.yapx.ru/dzgyG.png" />

Запуск стека

```
docker-compose up -d
```

</details>
<br/>

## Задание 7. WEB (HQ-SRV)

<br/>

<details>
<summary><strong> Настройка веб-приложения на HQ-SRV </strong></summary>

Установка необходимых компонентов:

```
apt install apache2 mariadb-server php php-mysql -y
```

Проверка

```
systemctl status apache2
```

```
systemctl status mariadb
```

Вхолд в MariaDB

```
mariadb
```

```
CREATE DATABASE webdb;
CREATE USER 'web1'@'localhost' IDENTIFIED BY 'P@ssw0rd';
GRANT ALL PRIVILEGES ON web1.* TO 'web'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

Импорт dump.sql

```
apt install apache2 mariadb-server php php-mysql -y
mariadb
mount /dev/sr0 /mnt/
cp /mnt/web/index.php /var/www/html/
cp /mnt/web/logo.png /var/www/html/
systemctl enable --now mariadb
cp /mnt/web/dump.sql
cp /mnt/web/dump.sql ./
mariadb -u web -p -D webdb < dump.sql
nano /var/www/html/index.php
systemctl restart apache2
mkdir -p /var/www/html/webapp
nano /var/www/html/index.php
cp /var/www/html/index.php /var/www/html/webapp/
cp /var/www/html/logo.png /var/www/html/webapp/
cd /var/www/html/webapp/
nano index.php
systemctl restart apache2
ls
nano index.php
systemctl restart apache2
```

Прописыва5ем в файле код:

```
<?php
$servername = "localhost";
$username = "web";
$password = "P@ssw0rd";
$dbname = "webls";

$conn = new mysqli($servername, $username, $password, $dbname);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>
```

Перезапуск:

```
systemctl restart apache2
```

</details>


<details>
<summary><strong> Проверка работы</strong></summary>

На клиенте:

```
http://HQ-SRV/webapp/index.php
```

На сервере

```
curl http://localhost/webapp/index.php
```
  
</details>

<br/>

## Задание 8. NAT (ПОРТЫ)

<br/>

<details>
<summary><strong> Настройка через IPTABLES </strong></summary>

### На HQ-RTR:

Настройка порта 8081:

```
iptables -t nat -A PREROUTING -i ens192 -p tcp --dport 8081 -j DNAT --to-destination 192.168.1.2:80
```

```
iptables -t nat -A POSTROUTING -o ens224 -p tcp --dport 80 -d 192.168.1.2 -j MASQUERADE
```

Настройка порта 2011 (SSH):

```
iptables -t nat -A PREROUTING -i ens192 -p tcp --dport 2011 -j DNAT --to-destination 192.168.1.2:2011
```

```
iptables -t nat -A POSTROUTING -o ens224 -p tcp --dport 2011 -d 192.168.1.2 -j MASQUERADE
```

### На BR-RTR

Настройка порта 8081:


```
iptables -t nat -A PREROUTING -i ens192 -p tcp --dport 8081 -j DNAT --to-destination 192.168.0.2:8081
```

```
iptables -t nat -A POSTROUTING -o ens224 -p tcp --dport 8081 -d 192.168.0.2 -j MASQUERADE
```

Настройка порта 2011:

```
iptables -t nat -A PREROUTING -i ens192 -p tcp --dport 2011 -j DNAT --to-destination 192.168.0.2:2011
```

```
iptables -t nat -A POSTROUTING -o ens224 -p tcp --dport 2011 -d 192.168.0.2 -j MASQUERADE
```

Сохраняем правила:
```
netfilter-persistent save
```

```
netfilter-persistent reload
```

Проверка портов:

# Проверка веб-приложения на HQ-SRV

```
curl http://172.16.10.2:8081
```
# Проверка Docker-приложения на BR-SRV ( НА КЛИЕНТА ЗАЙДИТЕ И АЙПИ ВЬЕБАШТЕ)

```
http://172.16.20.2:8081
```
</details>


## Задаие 9. NGINX PROXY (ISP)

<br/>

<details>
<summary><strong> Настройка на ISP  </strong></summary>

## Преднастройка

Переход к хостам:

```
nano /etc/hosts
```

Обозначение маршрутов:

```
192.168.1.2 HQ-SRV
192.168.0.2 BR-SRV
```

## Оснавная настройка

Установка необходимых пакетов:

```
apt update && apt install nginx -y
```
Запуск nginx:

```
systemctl enable nginx
```

```
systemctl start nginx
```

```
systemctl status nginx
```

Создание конфига прокси:

```
nano /etc/nginx/sites-available/au-team
```

Конфиг: ( СМОТРИТЕ auth_basic "Restricted Content";    auth_basic_user_file /etc/nginx/.htpasswd; они нужны для 10 задания если хотите проверить то закоментите их )
        

```
# Reverse proxy for HQ-SRV web application
server {
    listen 80;
    server_name web.au-team.irpo;

    location / {
        auth_basic "Restricted Content";   
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass http://172.16.10.2:8081;  # Resolves via /etc/hosts
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Reverse proxy for BR-SRV Docker web application
server {
    listen 80;
    server_name docker.au-team.irpo;

    location / {
        proxy_pass http://172.16.20.2:8081;  # Resolves via /etc/hosts
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

Активайция конфига:

```
 ln -s /etc/nginx/sites-available/au-team /etc/nginx/sites-enabled/
```

Проверка на ошибки

```
nginx -t
```

Презапуск:

```
systemctl restart nginx
```
</details>

<details>
<summary><strong> Настройка на HQ-CLI </strong></summary>

Настройка хостов:

```
nano /etc/hosts
```

```
172.16.10.1   web.au-team.irpo
172.16.10.1   docker.au-team.irpo
```

## Проверка

```
apt install curl
```

Проверка адресации:

```
curl http://web.au-team.irpo
```

```
curl http://docker.au-team.irpo
```
</details>


<br/>

### Задание 10 web-based аутентификация

<details>
<summary><strong> Настройка на ISP </strong></summary>

Установка необходимых пакетов

```
apt update && apt install apache2-utils -y
```

Создаём файл усчётных записей

```
htpasswd -bc /etc/nginx/.htpasswd Kharitonc P@ssw0rd
```

Проверка результата:

```
cat /etc/nginx/.htpasswd
```

Добавляем дополнительные строки в /etc/nginx/sites-available/au-team:

```
nano /etc/nginx/sites-available/au-team
```


>server {
>   
>   listen 80;
>   
>  server_name web.au-team.irpo;
>
>  location / {
>   
>  ```
>    auth_basic "Restricted Area";           
>    
>     auth_basic_user_file /etc/nginx/.htpasswd; 
>  ```
>  
>  proxy_pass http://<ВНЕШНИЙ_IP_HQ-RTR>:8080;
>      
>  proxy_set_header Host $host;
>       
>  proxy_set_header X-Real-IP $remote_addr;
>        
>  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
>   
>  }
>
> }

 Проверяем на ошибки:

```
 nginx -t
```

Перезагружаем 

```
systemctl reload nginx
```
  
</details>

<details>
<summary><strong> Проверка с HQ-CLI </strong></summary>

```
curl -I http://web.au-team.irpo
```

</details>

## Задание 11. ЯНДЕКС БРАУЗЕР (HQ-CLI)

<br/>

<details>
<summary><strong> Установка браузера </strong></summary>

```
apt install curl ca-certificates gpg -y
```

```
curl -fsSL https://repo.yandex.ru/yandex-browser/YANDEX-BROWSER-KEY.GPG | sudo gpg --dearmor --yes -o /usr/share/keyrings/yandex-browser.gpg
```

```
apt update
apt install yandex-browser-stable -y
```

Проверяем установку:

```
yandex-browser --version
```
  
</details>

<details>
<summary><strong> Проверка заруска </strong></summary>

Через командную строку:

```
yandex-browser
```

Через ярылк:

```
nano ~/.local/share/applications/yandex-browser.desktop
```

```
[Desktop Entry]
Version=1.0
Name=Yandex Browser
Comment=Browse the Web
Exec=/usr/bin/yandex-browser %U
Icon=yandex-browser
Terminal=false
Type=Application
Categories=Network;WebBrowser;

```
</details>

<br/>

